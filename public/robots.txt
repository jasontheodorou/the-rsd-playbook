# The RSD Playbook is a private internal site behind a password gate.
# Two routes sit outside that gate so they can be shared by URL, so keep
# them out of search results explicitly.
User-agent: *
Disallow: /tab-layouts
Disallow: /patterns
