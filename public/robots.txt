# The RSD Playbook is a private internal site behind a password gate.
# /tab-layouts sits outside that gate so it can be shared by URL, so keep
# it out of search results explicitly.
User-agent: *
Disallow: /tab-layouts
